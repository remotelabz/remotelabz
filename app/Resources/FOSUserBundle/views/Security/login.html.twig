{% extends 'base.html.twig' %}

{% block base %}

<div id="wrapper">
</div>
	<div class="row">
            <div class="col-md-4 col-md-offset-4">
				{% if error %}
				<div class="panel panel-red">
					<div class="panel-heading">
						<div>Erreur</div>
					</div>
					<div class="panel-body">
						{{ error.messageKey|trans(error.messageData, 'security') }}
					</div>
				</div>
				{% endif %}

				<div class="login-panel panel panel-default">
                    <div class="panel-heading">
                        <h3 class="panel-title">Veuillez vous connecter</h3>
						Compte de démonstration Etudiant : ttest79374/KWHjui7k8D<br/>
						Compte de démonstration Enseignant : ddemo-e489/nzs1jnCUql<br/>
                    </div>
                    <div class="panel-body">
                        <form action="{{ path('fos_user_security_check') }}" method="post">
                            <fieldset>
                                <div class="form-group">
                                    <input class="form-control" placeholder="Username" name="_username" type="text" autofocus value="{{ last_username }}"/>
                                </div>
                                <div class="form-group">
                                    <input class="form-control" placeholder="Mot de passe" name="_password" type="password" value=""/>
                                </div>
                                <div class="checkbox">
                                    <label>
                                        <input name="remember" type="checkbox" value="Remember Me">Se souvenir de moi
                                    </label>
                                </div>
								<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}"/>
                                <!-- Change this to a button or input when using this as a form -->
                                
								<input class="btn btn-lg btn-success btn-block" type="submit" value="Connexion"/>
								<a href="{{path('fos_user_resetting_request')}}" >Mot de passe oublié</a>
                            </fieldset>
                        </form>
                    </div>
                </div>
            </div>
	</div>
  
{% endblock %}
{% block footer %}
{% endblock %}