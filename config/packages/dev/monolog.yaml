monolog:
    handlers:
        main:
            type: stream
            path: "%kernel.logs_dir%/%kernel.environment%.log"
            level: debug
            channels: ["!event", "!doctrine", "!security", "!request", "!messenger", "!php", "!app"]
        security:
            type: stream
            path: "%kernel.logs_dir%/%kernel.environment%_security.log"
            level: info
            channels: ["security"]
        doctrine:
            type: stream
            path: "%kernel.logs_dir%/%kernel.environment%_doctrine.log"
            level: debug
            channels: ["doctrine"]
        app:
            type: stream
            path: "%kernel.logs_dir%/%kernel.environment%_app.log"
            level: debug
            channels: ["!event", "!doctrine", "!security", "!request", "!messenger", "!php"]
        messenger:
            type: stream
            path: "%kernel.logs_dir%/%kernel.environment%_messenger.log"
            level: debug
            channels: ["messenger"]
        php:
            type: stream
            path: "%kernel.logs_dir%/%kernel.environment%_php.log"
            level: debug
            channels: ["php"]
        request:
            type: stream
            path: "%kernel.logs_dir%/%kernel.environment%_request.log"
            level: debug
            channels: ["request"]
        event:
            type: stream
            path: "%kernel.logs_dir%/%kernel.environment%_event.log"
            level: debug
            channels: ["event"]
        # uncomment to get logging in your browser
        # you may have to allow bigger header sizes in your Web server configuration
        #firephp:
        #    type: firephp
        #    level: info
        #chromephp:
        #    type: chromephp
        #    level: info
        console:
            type: console
            process_psr_3_messages: false
            channels: ["!event", "!doctrine", "!console"]