{% extends 'dashboard.base.html.twig' %}

{% block wrapper %}
<div class="row">
    {% embed 'modal.html.twig' with {'title': 'New Network Interface'|trans, 'class': 'create-network-interface-modal'} %}
        {% block body %}
            {{ form(networkInterfaceForm, {'id': 'networkInterfaceForm'}) }}
        {% endblock %}
    {% endembed %}

    <div class="col grid-margin stretch-card">
        <div class="card">
            <div class="card-body">

                <h4 class="card-title">{{ 'Network Interfaces'|trans }}</h4>
                <p>
                    <button class="btn btn-success" data-target=".create-network-interface-modal" data-toggle="modal" type="button"><i
                            class="fas fa-plus"></i> {{ 'New'|trans }}</button>
                </p>
                <table id="networkInterfaceTable" class="table table-hover" style="width:100%">
                    <thead>
                        <tr>
                            <th>{{ 'Name'|trans }}</th>
                            <th>{{ 'Type'|trans }}</th>
                            <th>{{ 'Settings'|trans }}</th>
                            <th>{{ 'Device'|trans }}</th>
                        </tr>
                    </thead>
                    <tbody>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('network-interface') }}
{% endblock %}