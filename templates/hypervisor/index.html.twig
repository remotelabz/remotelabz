{% extends 'dashboard.base.html.twig' %}

{% block breadcrumbs %}
    {%- set breadcrumbs = breadcrumbs|merge({ 'Hypervisor': path('hypervisor') }) -%}
    {{ parent() }}
{% endblock %}

{% block sidebar %}
    {% set category = 'hypervisor' %}
    {{ parent() }}
{% endblock %}

{% block wrapper %}
<div class="top-panel">
    <div class="quick-actions">
        <a class="btn btn-success" role="button" href="{{ path('new_hypervisor') }}">
            New hypervisor
        </a>
    </div>
    <ul class="nav-links nav nav-tabs">
        <li class="active"><a href="#"><span>All</span> <span class="badge badge-pill">{{ hypervisor|length }}</span></a></li>
        {# <li><a href="#"><span>Images</span> <span class="badge badge-pill">0</span></a></li>
        <li><a href="#"><span>Links</span> <span class="badge badge-pill">0</span></a></li> #}
    </ul>
</div>
{# <div class="search-panel">
    <form action="{{ path('operating_systems') }}" method="GET">
        <input type="search" class="form-control input-sm" name="search"
        placeholder="Search by name"
        spellcheck="false" value="{{ search|default('') }}" type="search">
    </form>
</div> #}
<div class="labs-panel">
    {% for hypervisor in hypervisor %}
    <div class="content-list row align-items-center p-3 {% if not loop.last %}border-bottom{% endif %} lab-item">
            <div class="col-11">
                <a href="{{ path('show_hypervisor', {'id': hypervisor.id}) }}" class="lab-item-name">
                {{- hypervisor.name -}}
            </a>
            </div>
            
    </div>
    {% endfor %} 
</div>
{% endblock %}
    
{% block javascripts %}
    {{ parent() }}
    {# {{ encore_entry_script_tags('operating-system') }} #}
{% endblock %}